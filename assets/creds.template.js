'use strict';

/**
 * Copy this file:
 *
 * ```bash
 * cp creds.template.js creds.js
 * ```
 *
 * Then in the new `creds.js`, supply the values for your
 * Amazon email and password.
 *
 * The `creds.js` file is currently used by the scripts that retrieves your
 * activation bytes.  If you do not supply these values in `creds.js`, you
 * will be prompted to type them when they are needed by the script.
 */

module.exports = {
  email: '',
  password: '',
};
