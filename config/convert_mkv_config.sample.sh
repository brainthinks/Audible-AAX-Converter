# Where do you want the audiobook audio files saved?  The converted AAX files
# will have the extension `mkv`.
MKV_DIR="~/audible/mkv"

# The directory where the audiobooks will be placed after conversion to mp3.
# One directory will be created per audiobook inside of this directory.
MP3_DIR="~/audiobooks"
